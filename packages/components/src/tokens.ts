/**
 * The design tokens as CSS custom property references, for use in inline
 * styles or consumer-authored CSS-in-JS.
 *
 * @example
 * import { color, spacing } from '@jig-ui/react/tokens';
 * <div style={{ background: color.surfaces[100], padding: spacing[500] }} />
 */
export * from '@jig-ui/styles/tokens';
