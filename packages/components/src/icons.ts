/**
 * The icon set, kept out of the main entry so that importing a Button never
 * pulls the artwork in with it.
 *
 * @example
 * import { Icon } from '@jig-ui/react/icons';
 * <Icon icon="check-circle" weight="fill" label="Saved" />
 */
export * from './icon';
